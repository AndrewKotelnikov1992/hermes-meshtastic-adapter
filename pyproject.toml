[project]
name = "hermes-meshtastic-adapter"
version = "0.1.0"
description = "Hotplug-capable Meshtastic platform adapter for Hermes Agent"
requires-python = ">=3.11"
dependencies = ["meshtastic>=2.7,<3"]

[tool.pytest.ini_options]
testpaths = ["tests"]
